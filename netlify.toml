[build]
  command = "bun run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
  NITRO_PRESET = "netlify"
  # VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY must be set in
  # Site settings → Environment variables (they are baked into the client
  # bundle at build time).