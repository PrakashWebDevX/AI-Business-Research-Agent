# Backend URL for the AI Business Research Agent (FastAPI + LangChain).
# Local development:
VITE_API_URL=http://localhost:8000

# Production example (set in Netlify → Site settings → Environment variables):
# VITE_API_URL=https://api.your-domain.com
